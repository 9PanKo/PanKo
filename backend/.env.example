# Copy to .env locally. On Railway: Project → backend service → Variables.
GEMINI_API_KEY=your_gemini_api_key
# Optional: override default Gemini models (comma-separated not supported; single model name)
# GEMINI_MODEL=gemini-2.5-flash
